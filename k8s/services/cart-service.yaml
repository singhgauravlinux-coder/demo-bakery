apiVersion: apps/v1
kind: Deployment
metadata:
  name: cart-service
  namespace: bakery
  labels:
    app: cart-service
    app.kubernetes.io/part-of: crumb-and-ember
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cart-service
  template:
    metadata:
      labels:
        app: cart-service
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: cart-service
          image: ghcr.io/singhgauravlinux-coder/demo-bakery/cart-service:4e7ca01
          imagePullPolicy: Always
          ports:
            - containerPort: 3006
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          env:
            - name: SERVICE_NAME
              value: "cart-service"
            - name: PORT
              value: "3006"
            - name: LOG_LEVEL
              value: "info"
            - name: REDIS_URL
              value: "redis://redis:6379"
          readinessProbe:
            httpGet:
              path: /ready
              port: 3006
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 3006
            initialDelaySeconds: 10
            periodSeconds: 20
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 250m
              memory: 128Mi
---
apiVersion: v1
kind: Service
metadata:
  name: cart-service
  namespace: bakery
  labels:
    app: cart-service
spec:
  selector:
    app: cart-service
  ports:
    - port: 3006
      targetPort: 3006
      protocol: TCP
