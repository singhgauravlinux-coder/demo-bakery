apiVersion: apps/v1
kind: Deployment
metadata:
  name: product-catalog-service
  namespace: bakery
  labels:
    app: product-catalog-service
    app.kubernetes.io/part-of: crumb-and-ember
spec:
  replicas: 2
  selector:
    matchLabels:
      app: product-catalog-service
  template:
    metadata:
      labels:
        app: product-catalog-service
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: product-catalog-service
          image: ghcr.io/singhgauravlinux-coder/demo-bakery/product-catalog-service:4e7ca01
          imagePullPolicy: Always
          ports:
            - containerPort: 3003
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          env:
            - name: SERVICE_NAME
              value: "product-catalog-service"
            - name: PORT
              value: "3003"
            - name: LOG_LEVEL
              value: "info"
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: bakery-db-secret
                  key: DATABASE_URL
          readinessProbe:
            httpGet:
              path: /ready
              port: 3003
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 3003
            initialDelaySeconds: 10
            periodSeconds: 20
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 250m
              memory: 128Mi
---
apiVersion: v1
kind: Service
metadata:
  name: product-catalog-service
  namespace: bakery
  labels:
    app: product-catalog-service
spec:
  selector:
    app: product-catalog-service
  ports:
    - port: 3003
      targetPort: 3003
      protocol: TCP
