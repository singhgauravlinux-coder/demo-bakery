apiVersion: apps/v1
kind: Deployment
metadata:
  name: media-service
  namespace: bakery
  labels:
    app: media-service
    app.kubernetes.io/part-of: crumb-and-ember
spec:
  replicas: 1
  selector:
    matchLabels:
      app: media-service
  template:
    metadata:
      labels:
        app: media-service
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: media-service
          image: ghcr.io/singhgauravlinux-coder/demo-bakery/media-service:4e7ca01
          imagePullPolicy: Always
          ports:
            - containerPort: 3020
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          env:
            - name: SERVICE_NAME
              value: "media-service"
            - name: PORT
              value: "3020"
            - name: LOG_LEVEL
              value: "info"
          readinessProbe:
            httpGet:
              path: /ready
              port: 3020
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 3020
            initialDelaySeconds: 10
            periodSeconds: 20
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 250m
              memory: 128Mi
---
apiVersion: v1
kind: Service
metadata:
  name: media-service
  namespace: bakery
  labels:
    app: media-service
spec:
  selector:
    app: media-service
  ports:
    - port: 3020
      targetPort: 3020
      protocol: TCP
