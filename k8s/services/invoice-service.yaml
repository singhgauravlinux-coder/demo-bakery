apiVersion: apps/v1
kind: Deployment
metadata:
  name: invoice-service
  namespace: bakery
  labels:
    app: invoice-service
    app.kubernetes.io/part-of: crumb-and-ember
spec:
  replicas: 1
  selector:
    matchLabels:
      app: invoice-service
  template:
    metadata:
      labels:
        app: invoice-service
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: invoice-service
          image: ghcr.io/singhgauravlinux-coder/demo-bakery/invoice-service:4e7ca01
          imagePullPolicy: Always
          ports:
            - containerPort: 3021
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          env:
            - name: SERVICE_NAME
              value: "invoice-service"
            - name: PORT
              value: "3021"
            - name: LOG_LEVEL
              value: "info"
          readinessProbe:
            httpGet:
              path: /ready
              port: 3021
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 3021
            initialDelaySeconds: 10
            periodSeconds: 20
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 250m
              memory: 128Mi
---
apiVersion: v1
kind: Service
metadata:
  name: invoice-service
  namespace: bakery
  labels:
    app: invoice-service
spec:
  selector:
    app: invoice-service
  ports:
    - port: 3021
      targetPort: 3021
      protocol: TCP
