apiVersion: apps/v1
kind: Deployment
metadata:
  name: promotion-service
  namespace: bakery
  labels:
    app: promotion-service
    app.kubernetes.io/part-of: crumb-and-ember
spec:
  replicas: 1
  selector:
    matchLabels:
      app: promotion-service
  template:
    metadata:
      labels:
        app: promotion-service
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: promotion-service
          image: ghcr.io/singhgauravlinux-coder/demo-bakery/promotion-service:4e7ca01
          imagePullPolicy: Always
          ports:
            - containerPort: 3014
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          env:
            - name: SERVICE_NAME
              value: "promotion-service"
            - name: PORT
              value: "3014"
            - name: LOG_LEVEL
              value: "info"
          readinessProbe:
            httpGet:
              path: /ready
              port: 3014
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 3014
            initialDelaySeconds: 10
            periodSeconds: 20
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 250m
              memory: 128Mi
---
apiVersion: v1
kind: Service
metadata:
  name: promotion-service
  namespace: bakery
  labels:
    app: promotion-service
spec:
  selector:
    app: promotion-service
  ports:
    - port: 3014
      targetPort: 3014
      protocol: TCP
