apiVersion: apps/v1
kind: Deployment
metadata:
  name: search-service
  namespace: bakery
  labels:
    app: search-service
    app.kubernetes.io/part-of: crumb-and-ember
spec:
  replicas: 1
  selector:
    matchLabels:
      app: search-service
  template:
    metadata:
      labels:
        app: search-service
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: search-service
          image: ghcr.io/singhgauravlinux-coder/demo-bakery/search-service:4e7ca01
          imagePullPolicy: Always
          ports:
            - containerPort: 3012
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          env:
            - name: SERVICE_NAME
              value: "search-service"
            - name: PORT
              value: "3012"
            - name: LOG_LEVEL
              value: "info"
          readinessProbe:
            httpGet:
              path: /ready
              port: 3012
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 3012
            initialDelaySeconds: 10
            periodSeconds: 20
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 250m
              memory: 128Mi
---
apiVersion: v1
kind: Service
metadata:
  name: search-service
  namespace: bakery
  labels:
    app: search-service
spec:
  selector:
    app: search-service
  ports:
    - port: 3012
      targetPort: 3012
      protocol: TCP
