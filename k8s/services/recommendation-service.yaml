apiVersion: apps/v1
kind: Deployment
metadata:
  name: recommendation-service
  namespace: bakery
  labels:
    app: recommendation-service
    app.kubernetes.io/part-of: crumb-and-ember
spec:
  replicas: 1
  selector:
    matchLabels:
      app: recommendation-service
  template:
    metadata:
      labels:
        app: recommendation-service
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: recommendation-service
          image: ghcr.io/singhgauravlinux-coder/demo-bakery/recommendation-service:4e7ca01
          imagePullPolicy: Always
          ports:
            - containerPort: 3013
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          env:
            - name: SERVICE_NAME
              value: "recommendation-service"
            - name: PORT
              value: "3013"
            - name: LOG_LEVEL
              value: "info"
          readinessProbe:
            httpGet:
              path: /ready
              port: 3013
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 3013
            initialDelaySeconds: 10
            periodSeconds: 20
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 250m
              memory: 128Mi
---
apiVersion: v1
kind: Service
metadata:
  name: recommendation-service
  namespace: bakery
  labels:
    app: recommendation-service
spec:
  selector:
    app: recommendation-service
  ports:
    - port: 3013
      targetPort: 3013
      protocol: TCP
