apiVersion: apps/v1
kind: Deployment
metadata:
  name: review-service
  namespace: bakery
  labels:
    app: review-service
    app.kubernetes.io/part-of: crumb-and-ember
spec:
  replicas: 1
  selector:
    matchLabels:
      app: review-service
  template:
    metadata:
      labels:
        app: review-service
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: review-service
          image: ghcr.io/singhgauravlinux-coder/demo-bakery/review-service:4e7ca01
          imagePullPolicy: Always
          ports:
            - containerPort: 3011
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          env:
            - name: SERVICE_NAME
              value: "review-service"
            - name: PORT
              value: "3011"
            - name: LOG_LEVEL
              value: "info"
          readinessProbe:
            httpGet:
              path: /ready
              port: 3011
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 3011
            initialDelaySeconds: 10
            periodSeconds: 20
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 250m
              memory: 128Mi
---
apiVersion: v1
kind: Service
metadata:
  name: review-service
  namespace: bakery
  labels:
    app: review-service
spec:
  selector:
    app: review-service
  ports:
    - port: 3011
      targetPort: 3011
      protocol: TCP
