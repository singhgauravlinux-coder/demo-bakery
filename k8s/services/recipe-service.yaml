apiVersion: apps/v1
kind: Deployment
metadata:
  name: recipe-service
  namespace: bakery
  labels:
    app: recipe-service
    app.kubernetes.io/part-of: crumb-and-ember
spec:
  replicas: 1
  selector:
    matchLabels:
      app: recipe-service
  template:
    metadata:
      labels:
        app: recipe-service
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: recipe-service
          image: ghcr.io/singhgauravlinux-coder/demo-bakery/recipe-service:4e7ca01
          imagePullPolicy: Always
          ports:
            - containerPort: 3016
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          env:
            - name: SERVICE_NAME
              value: "recipe-service"
            - name: PORT
              value: "3016"
            - name: LOG_LEVEL
              value: "info"
          readinessProbe:
            httpGet:
              path: /ready
              port: 3016
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 3016
            initialDelaySeconds: 10
            periodSeconds: 20
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 250m
              memory: 128Mi
---
apiVersion: v1
kind: Service
metadata:
  name: recipe-service
  namespace: bakery
  labels:
    app: recipe-service
spec:
  selector:
    app: recipe-service
  ports:
    - port: 3016
      targetPort: 3016
      protocol: TCP
