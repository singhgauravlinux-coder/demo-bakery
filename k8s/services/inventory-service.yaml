apiVersion: apps/v1
kind: Deployment
metadata:
  name: inventory-service
  namespace: bakery
  labels:
    app: inventory-service
    app.kubernetes.io/part-of: crumb-and-ember
spec:
  replicas: 1
  selector:
    matchLabels:
      app: inventory-service
  template:
    metadata:
      labels:
        app: inventory-service
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: inventory-service
          image: ghcr.io/singhgauravlinux-coder/demo-bakery/inventory-service:4e7ca01
          imagePullPolicy: Always
          ports:
            - containerPort: 3004
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          env:
            - name: SERVICE_NAME
              value: "inventory-service"
            - name: PORT
              value: "3004"
            - name: LOG_LEVEL
              value: "info"
          readinessProbe:
            httpGet:
              path: /ready
              port: 3004
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 3004
            initialDelaySeconds: 10
            periodSeconds: 20
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 250m
              memory: 128Mi
---
apiVersion: v1
kind: Service
metadata:
  name: inventory-service
  namespace: bakery
  labels:
    app: inventory-service
spec:
  selector:
    app: inventory-service
  ports:
    - port: 3004
      targetPort: 3004
      protocol: TCP
