apiVersion: apps/v1
kind: Deployment
metadata:
  name: currency-service
  namespace: bakery
  labels:
    app: currency-service
    app.kubernetes.io/part-of: crumb-and-ember
spec:
  replicas: 1
  selector:
    matchLabels:
      app: currency-service
  template:
    metadata:
      labels:
        app: currency-service
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: currency-service
          image: ghcr.io/singhgauravlinux-coder/demo-bakery/currency-service:4e7ca01
          imagePullPolicy: Always
          ports:
            - containerPort: 3022
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          env:
            - name: SERVICE_NAME
              value: "currency-service"
            - name: PORT
              value: "3022"
            - name: LOG_LEVEL
              value: "info"
          readinessProbe:
            httpGet:
              path: /ready
              port: 3022
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 3022
            initialDelaySeconds: 10
            periodSeconds: 20
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 250m
              memory: 128Mi
---
apiVersion: v1
kind: Service
metadata:
  name: currency-service
  namespace: bakery
  labels:
    app: currency-service
spec:
  selector:
    app: currency-service
  ports:
    - port: 3022
      targetPort: 3022
      protocol: TCP
