apiVersion: apps/v1
kind: Deployment
metadata:
  name: delivery-service
  namespace: bakery
  labels:
    app: delivery-service
    app.kubernetes.io/part-of: crumb-and-ember
spec:
  replicas: 1
  selector:
    matchLabels:
      app: delivery-service
  template:
    metadata:
      labels:
        app: delivery-service
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: delivery-service
          image: ghcr.io/singhgauravlinux-coder/demo-bakery/delivery-service:4e7ca01
          imagePullPolicy: Always
          ports:
            - containerPort: 3009
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          env:
            - name: SERVICE_NAME
              value: "delivery-service"
            - name: PORT
              value: "3009"
            - name: LOG_LEVEL
              value: "info"
          readinessProbe:
            httpGet:
              path: /ready
              port: 3009
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 3009
            initialDelaySeconds: 10
            periodSeconds: 20
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 250m
              memory: 128Mi
---
apiVersion: v1
kind: Service
metadata:
  name: delivery-service
  namespace: bakery
  labels:
    app: delivery-service
spec:
  selector:
    app: delivery-service
  ports:
    - port: 3009
      targetPort: 3009
      protocol: TCP
